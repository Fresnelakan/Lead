specVersion: "v1"
serviceId: "lead"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lead-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
